        // Inicialización suave de AOS
        AOS.init({
          duration: 1200, // Duración de la animación
          easing: "ease-out-quad", // Movimiento suave sin rebote
          once: true, // Se ejecuta solo una vez al hacer scroll
        });